header 
    a(href="/").header__logo
        i.fab.fa-youtube
        div.bg
    nav 
        ul 
            li 
                a(href="/search") 
                    i.fas.fa-search
            if loggedIn
                li 
                    a(href="/videos/upload") Upload video
                li 
                    a(href=`/users/${user._id}`) My Profile
                li 
                    a(href="/users/edit") Edit Profile
                li 
                    a(href="/users/logout") Log Out
                li
                    a(href=`/users/${user._id}`)
                        if (user.avatarUrl === undefined || user.avatarUrl === "")
                            span 😊
                        else 
                            img.header__avatar(src="/" + user.avatarUrl)
            else
                li 
                    a(href="/login") Login
                li 
                    a(href="/join").header__btn  Join